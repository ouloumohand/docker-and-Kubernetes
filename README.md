# FastAPI + Playwright + pytest Demo

This project includes:
- FastAPI app
- pytest API tests
- Playwright UI tests
- Dockerfile + docker-compose
- Kubernetes manifests
- GitHub Actions CI
- Jenkins CI

## Local run

```bash
uvicorn app.main:app --reload
